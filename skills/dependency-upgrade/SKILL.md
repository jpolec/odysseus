---
name: dependency-upgrade
description: Upgrade dependencies with scoped compatibility research, lockfile discipline, migration review, security awareness, and targeted regression checks.
triggers: dependency, dependencies, upgrade, update package, npm, pnpm, yarn, pip, uv, cargo, version, lockfile, deprecation
---

# Dependency Upgrade

Confirm the requested version range and inspect upstream migration notes when available. Change the smallest coherent dependency set and keep generated lockfiles consistent. Search for removed or deprecated APIs. Run the package's focused tests plus build/type checks. Do not mix unrelated formatting or refactors into the upgrade, and record any manual migration still required.
